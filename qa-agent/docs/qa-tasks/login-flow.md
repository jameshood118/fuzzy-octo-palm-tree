# Target: The Airlock (Login Flow)

Navigate to the root URL `/`. Since the browser is unauthenticated, verify that the system's AuthGuard automatically redirects the browser to the `/login` route.

Once successfully redirected to the login page, verify the physical presence of the "SAFEHOOD PROTOCOL" header text, and ensure the "Initialize Uplink" button is visible on the DOM.
